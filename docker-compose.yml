version: "3.9"

services:
  mongodb:
    container_name: iot_power_company_mongodb
    image: mongo:latest
    command: mongod --auth
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongodb
      MONGO_INITDB_ROOT_PASSWORD: mongodb
      MONGO_INITDB_DATABASE: mongodb
      MONGODB_DATA_DIR: /data/db/main
      MONGODB_LOG_DIR: /dev/null
    volumes:
      - ./data/main:/data/db
    ports:
      - 27017:27017
    networks:
      - mongodb

  redis:
    container_name: iot_power_company_redis
    image: redis/redis-stack:latest
    ports:
      - 6379:6379
      - 8801:8001 # RedisInsight
    networks:
      - redis

  api:
    container_name: iot_power_company_api
    build: ./api
    ports:
      - 8000:8000
    depends_on:
      - mongodb
      - redis
    volumes:
      - ./api:/app/api/
    networks:
      - mongodb
      - redis

  consumer-web:
    container_name: iot_power_company_consumer_web
    build: ./consumer-web
    ports:
      - 3000:3000
    volumes:
      - ./consumer-web:/app/consumer-web
    depends_on:
      - api

  employee-web:
    container_name: iot_power_company_employee_web
    build: ./employee-web
    ports:
      - 3001:3000
    volumes:
      - ./employee-web:/app/employee-web
    depends_on:
      - api

networks:
  mongodb:
    driver: bridge
  redis:
    driver: bridge
